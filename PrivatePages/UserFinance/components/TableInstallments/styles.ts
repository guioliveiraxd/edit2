import styled from 'styled-components';
import * as RTable from 'react-super-responsive-table';

export const StyledTable = styled(RTable.Table)`
  width: 100%;
`;

export const StyledTd = styled(RTable.Td)`
  padding: 1rem;
  text-align: center;
  font-weight: 600;
`;
