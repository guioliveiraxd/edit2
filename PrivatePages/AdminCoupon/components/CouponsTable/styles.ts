import styled from 'styled-components';

export const ActionsContainer = styled.div`
  display: flex;
  flex-direction: row;
  gap: 22px;

  > svg {
    font-size: 20px;
    cursor: pointer;
  }
`;
